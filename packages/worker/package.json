{
  "name": "@aiusage/worker",
  "version": "1.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/0verme/aiusage.git",
    "directory": "packages/worker"
  },
  "type": "module",
  "scripts": {
    "predeploy": "node scripts/build.mjs",
    "deploy": "wrangler deploy",
    "postdeploy": "node -e \"require('node:fs').rmSync('./public',{recursive:true,force:true})\"",
    "predev": "node scripts/build.mjs",
    "dev": "wrangler dev",
    "postdev": "node -e \"require('node:fs').rmSync('./public',{recursive:true,force:true})\"",
    "build": "node scripts/build.mjs",
    "lint": "tsc --noEmit",
    "types": "wrangler types",
    "test": "vitest run && node --test scripts/*.test.mjs",
    "db:migrate:local": "wrangler d1 execute aiusage-db --local --file=migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute aiusage-db --remote --file=migrations/0001_init.sql",
    "db:recalculate": "node --import tsx/esm scripts/recalculate-costs.ts"
  },
  "dependencies": {
    "@aiusage/dashboard": "workspace:*",
    "@aiusage/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^4.1.2",
    "wrangler": "^4.80.0"
  }
}
